package:
  name: automation-service-test
  dependencies:
    - automation-service
    - spec-node
  bundle:
    module: Test.Runner
    outfile: "test.mjs"
    platform: node
    type: "app"
    extraArgs:
      - "--external:playwright"
      - "--external:playwright-core"
